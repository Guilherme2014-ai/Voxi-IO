query getChatByIDQuery($chat_id: ID!) {
    chat(where: {id: $chat_id}) {
        contacts {
            name
            username
            profile_picture_url
            bio
        }
        messages {
            text
            contactSenderUsername
        }
    }
}
