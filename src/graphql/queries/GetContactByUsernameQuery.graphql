query GetContactByUsernameQuery($username: String!) {
    contact(where: { username: $username }) {
        name
        username

        chats {
            id
            contacts {
                name
                profile_picture_url
            }
            messages {
                text
            }
        }
    }
}